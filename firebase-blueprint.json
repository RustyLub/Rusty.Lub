{
  "entities": {
    "Message": {
      "title": "Message",
      "description": "A chat message sent by a survivor",
      "type": "object",
      "properties": {
        "text": {
          "type": "string",
          "description": "The message content"
        },
        "uid": {
          "type": "string",
          "description": "The sender's GitHub UID"
        },
        "displayName": {
          "type": "string",
          "description": "The sender's display name"
        },
        "photoURL": {
          "type": "string",
          "description": "The sender's avatar URL"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the message was created"
        }
      },
      "required": ["text", "uid", "displayName", "createdAt"]
    },
    "Presence": {
      "title": "Presence",
      "description": "The online presence status of an authorized user",
      "type": "object",
      "properties": {
        "lastActive": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp of the user's last active heartbeat"
        },
        "displayName": {
          "type": "string",
          "description": "The display name of the online user"
        }
      },
      "required": ["lastActive"]
    },
    "ChatUser": {
      "title": "ChatUser",
      "description": "User profile data for chat and management",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "username": { "type": "string" },
        "displayName": { "type": "string" },
        "photoURL": { "type": "string" },
        "avatarClass": { "type": "string" },
        "role": { "type": "string" },
        "isBlocked": { "type": "boolean" },
        "isVip": { "type": "boolean" },
        "steamUrl": { "type": "string" }
      }
    }
  },
  "firestore": {
    "/messages/{messageId}": {
      "schema": "Message",
      "description": "Global chat messages collection"
    },
    "/presence/{userId}": {
      "schema": "Presence",
      "description": "Real-time presence collection"
    },
    "/chat_users/{username}": {
      "schema": "ChatUser",
      "description": "Registered chat users"
    }
  }
}
